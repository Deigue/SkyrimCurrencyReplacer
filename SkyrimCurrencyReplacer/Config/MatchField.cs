namespace SkyrimCurrencyReplacer.Config
{
    public struct MatchField
    {
        // if match type is string, init corresponding exact/contain arrays.
        // if match type is along lines of FormId/FormKey, then try match exactly.
        
        // TODO: Arrays perhaps need to be lazy init, or declared optional perhaps.
        // Possibly Generic type.? always have props Dwemer,Nordic etc but of diff type.
        
    }
}